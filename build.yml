platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}
  type: docker-image
  source: {repository: binded/python-opencv}
  type: docker-image
  source: {repository: python}

inputs:
- name: src-face

run:
  path: ./src-face/build
